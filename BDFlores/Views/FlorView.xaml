<UserControl x:Class="BDFlores.Views.FlorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BDFlores.Views"
             xmlns:viewmodel="clr-namespace:BDFlores.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Center"></Setter>
            </Style>
        </StackPanel.Resources>
        <TextBlock Text="{Binding Nombre}" FontWeight="Bold"></TextBlock>
        <Image  HorizontalAlignment="Center" Source="{Binding URLImagen}" Height="100" Stretch="Uniform"></Image>
        <TextBlock FontSize="10" Text="{Binding NombreCientífico}"></TextBlock>
    </StackPanel>
</UserControl>
